import { ControlledForm } from '../../components/forms/ControlledForm/ReactHookForm';

export const ControlledFormPage = () => {
  return (
    <section className="controlled-form">
      <ControlledForm />
    </section>
  );
};
